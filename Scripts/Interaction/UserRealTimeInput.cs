namespace Revistone
{
    namespace Interaction
    {
        /// <summary> [WIP] Class for dealing with realtime input from the user, use for higher controller </summary>
        public static class UserRealtimeInput
        {
            /// will code this at some point to be able to handle realtime shit but for now chilling like this
        }
    }
}
